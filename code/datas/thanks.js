import { Animatable } from 'framer'

const opacity = Animatable(0)
// const scale = Animatable(1)
const top = Animatable(-50)
const left = Animatable(0)

export default {
  opacity,
  // scale,
  top,
  left,
}
